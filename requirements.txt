# Production dependencies for the public Vercel demo deployment (DEMO_MODE=1).
# Function entry: api/index.py — mounts backend.app (see vercel.json).
# NO SQLAlchemy / psycopg2 / python-dotenv here: TASK 3 removed the DB from the demo
# reachable import graph, and settings.DEMO_MODE gates the operational routers behind
# a deferred import so backend.database is never touched.
#
# If you need the operational stack locally (DEMO_MODE=0), use backend/requirements.txt
# instead (dev environment — includes SQLAlchemy/psycopg2/black/ruff).
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.11.0
fastapi==0.122.0
h11==0.16.0
idna==3.11
numpy==2.2.0
packaging==25.0
pandas==2.3.3
pydantic==2.12.4
pydantic-settings==2.12.0
pydantic_core==2.41.5
python-dateutil==2.9.0.post0
pytz==2025.2
six==1.17.0
sniffio==1.3.1
starlette==0.50.0
typing-inspection==0.4.2
typing_extensions==4.15.0
tzdata==2025.2
uvicorn==0.38.0
